version: "3.9"

services:
  web:
    build: .
    image: rss-analysis
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./public:/app/public
